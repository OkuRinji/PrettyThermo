# Зависимости для PrettyThermo
# Оба приложения используют только стандартную библиотеку Python
# PyInstaller и matplotlib требуются только для сборки .exe файлов

# Для сборки исполняемых файлов:
pyinstaller>=6.0.0

# Для построения графиков:
matplotlib>=3.7.0
